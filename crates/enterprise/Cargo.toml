[package]
name = "diavlos-enterprise"
description = "Diavlos enterprise crate. Empty on purpose. Exists so open-core is a config change later, not a refactor."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
